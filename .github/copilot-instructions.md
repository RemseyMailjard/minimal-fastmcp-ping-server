# Project instructions

- Keep this MCP server intentionally minimal: it exposes only the `ping` tool.
- Manage Python and dependencies with uv.
- Follow the [FastMCP documentation](https://gofastmcp.com/) and the [official MCP Python SDK](https://github.com/modelcontextprotocol/python-sdk) when changing protocol behavior.
- Run `uv run pytest` after changes.